spring:
  cloud:
    loadbalancer:
      nacos:
        enabled: true
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
      discovery:
        server-addr: 127.0.0.1:8848
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              - GET
              - PUT
              - DELETE
              - POST
              - PATCH
              - OPTIONS
      discovery:
        locator:
          enabled: true
      routes:
        - id: user-center
          uri: lb://user-service
          predicates:
            - Path=/user/**
knife4j:
  gateway:
    enabled: true
    discover:
      enabled: true
      excluded-services: gateway,authorization_service
logging:
  level:
    org.springframework.security: trace